---
status: proposed
---

# 对运行期单路故障采用有界恢复

程序已经成功启动后，RTSP 读取超时、EOF、网络或 I/O 错误，以及可恢复的 VDEC 故障，不再立即终止全部相机路，而是只重建故障相机路；每路独立拥有 3 次恢复机会，第 1 次立即执行，后续失败后间隔 30 秒，产生首个健康解码画面后恢复运行并在稳定 30 秒后重置预算，任意一路耗尽预算则协调关闭整个程序并返回失败。初次启动失败、输入规格不兼容、内部状态错误、单路资源清理失败以及共享处理故障仍立即全局失败；该方案在单错隔离能力与避免无限恢复之间选择了有界恢复。
